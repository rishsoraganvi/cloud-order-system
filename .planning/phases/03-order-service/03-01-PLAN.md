---
phase: 03-order-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/order-service/app/main.py
  - services/order-service/app/models.py
  - services/order-service/app/schemas.py
  - services/order-service/app/repository.py
  - services/order-service/app/service.py
  - services/order-service/app/router.py
  - services/order-service/app/events.py
  - services/order-service/tests/test_orders.py
  - services/order-service/Dockerfile
  - services/order-service/requirements.txt
autonomous: true
requirements: [ORDER-01, ORDER-02, ORDER-03, ORDER-04]
must_haves:
  truths:
    - "POST /orders places order, calls Product Service to reserve stock, publishes event to RabbitMQ"
    - "GET /orders/{id} returns order status (PENDING, CONFIRMED, SHIPPED, DELIVERED, CANCELLED)"
    - "PATCH /orders/{id}/status validates state transitions per state machine, rejects invalid transitions with 400"
    - "State transitions follow directed graph: PENDING→{CONFIRMED,CANCELLED}, CONFIRMED→SHIPPED, SHIPPED→DELIVERED"
  artifacts:
    - path: "services/order-service/app/models.py"
      provides: "SQLAlchemy Order model with status field and state transition logic"
    - path: "services/order-service/app/service.py"
      provides: "Business logic: place order, HTTP call to Product Service, state machine validation"
    - path: "services/order-service/app/events.py"
      provides: "RabbitMQ publisher for order.placed events (Observer Pattern)"
    - path: "services/order-service/app/router.py"
      provides: "REST endpoints: POST /orders, GET /orders/{id}, PATCH /orders/{id}/status"
    - path: "services/order-service/tests/test_orders.py"
      provides: "Tests for order placement, state transitions, RabbitMQ publishing"
  key_links:
    - from: "services/order-service/app/router.py"
      to: "services/order-service/app/service.py"
      via: "HTTP handlers invoke service methods"
    - from: "services/order-service/app/service.py"
      to: "services/order-service/app/events.py"
      via: "After successful order creation, service publishes event"
    - from: "services/order-service/app/service.py"
      to: "Product Service (port 8002)"
      via: "HTTP PUT /products/{id}/reserve for stock check"
    - from: "services/order-service/app/service.py"
      to: "services/order-service/app/repository.py"
      via: "Service calls repository to persist order"

---

Objective
---------
Create the Order Service (FastAPI) core: models, business logic with state machine, inter-service communication to Product Service, and RabbitMQ event publishing. Deliver tested, Dockerized service ready for Notification Service integration.

Purpose: Central orchestrator for order workflow. Calls Product Service for inventory; publishes events for Notification Service.
Output: Source files, tests passing, state machine validation working, Docker image.

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/initial_research.md
</execution_context>

<context>
See `full_build_plan.md` Phase 3 section. Order Service depends on Product Service existing (Phase 2) for stock reservation calls.
State machine is a DSA concept (directed graph): implement as dict of valid transitions.
Observer Pattern: when order placed, events.py publishes to RabbitMQ; Notification Service (Phase 4) consumes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define models, state machine, and write failing tests (RED)</name>
  <files>services/order-service/app/models.py, services/order-service/app/schemas.py, services/order-service/app/repository.py, services/order-service/tests/test_orders.py</files>
  <behavior>
    - Test 1: POST /orders with valid product_id and qty calls Product Service reserve; if success, order created with PENDING status.
    - Test 2: GET /orders/{id} returns order with status; invalid id returns 404.
    - Test 3: PATCH /orders/{id}/status with valid transition (PENDING→CONFIRMED) returns 200; invalid transition returns 400.
    - Test 4: Invalid state transition (PENDING→DELIVERED) is rejected with 400.
  </behavior>
  <action>
    - Create `models.py` with Order entity: id, product_id, qty, status, created_at, updated_at.
    - Create `schemas.py` with OrderCreate, OrderUpdate, OrderResponse Pydantic models.
    - Create state machine dict in `service.py`: VALID_TRANSITIONS = {PENDING: [CONFIRMED, CANCELLED], ...}.
    - Create `repository.py` with create_order, get_order, update_order_status.
    - Create `tests/test_orders.py` with the four tests above using TestClient and mock Product Service calls.
  </action>
  <verify>
    <automated>pytest services/order-service/tests/test_orders.py -q</automated>
  </verify>
  <done>Models and schemas created, state machine dict present, tests written and run (RED state expected).</done>
</task>

<task type="auto">
  <name>Task 2: Implement service logic, HTTP call to Product Service, RabbitMQ publishing, and make tests pass (GREEN)</name>
  <files>services/order-service/app/service.py, services/order-service/app/router.py, services/order-service/app/events.py, services/order-service/app/main.py, services/order-service/Dockerfile, services/order-service/requirements.txt</files>
  <action>
    - Implement `service.py` with:
      * `place_order(db, product_id, qty)`: calls Product Service via httpx, catches 409 (stock error), saves order if success, publishes event.
      * `get_order_status(db, order_id)`: returns order or None.
      * `update_order_status(db, order_id, new_status)`: validates transition using VALID_TRANSITIONS dict, updates or raises 400.
    - Implement `events.py` with RabbitMQ publisher function (use pika or aio_pika) to publish order.placed events.
    - Implement `router.py` with endpoints for POST /orders, GET /orders/{id}, PATCH /orders/{id}/status.
    - Add `requirements.txt` with fastapi, sqlalchemy, pika (or aio_pika), httpx.
    - Add `Dockerfile` with multi-stage Python build.
    - Configure `main.py` to initialize RabbitMQ connection.
  </action>
  <verify>
    <automated>pytest services/order-service/tests/test_orders.py -q</automated>
  </verify>
  <done>All tests pass; state machine validates transitions; HTTP call to Product Service works; RabbitMQ publisher ready; Dockerfile builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Client -> API Gateway -> Order Service (untrusted input)
Order Service -> Product Service (internal)
Order Service -> RabbitMQ (internal)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | I | POST /orders payload | mitigate | Validate product_id and qty with Pydantic; reject negative qty. |
| T-02 | T | HTTP call to Product Service | mitigate | Use timeout on httpx client (e.g., 5s); handle connection errors gracefully. |
| T-03 | T | State transition | mitigate | Validate new_status against VALID_TRANSITIONS dict; log invalid attempts. |
| T-04 | R | RabbitMQ publishing | mitigate | Ensure message is published atomically after order commit; handle publish failures. |
</threat_model>

<verification>
- Run `pytest services/order-service/tests/test_orders.py` — tests must pass.
- Mock Product Service in tests; verify HTTP call logic with expected payloads.
- Build Docker image: `docker build -t order-service:local services/order-service` — image builds successfully.
</verification>

<success_criteria>
1. Tests covering order placement, status updates, and state machine validation exist and pass.
2. HTTP call to Product Service works (with mock in tests, real endpoint in deployment).
3. RabbitMQ event publishing implemented and tested.
4. Service is Dockerized.
</success_criteria>

<output>
After completion, create `.planning/phases/03-order-service/03-01-SUMMARY.md` with test results and artifacts list.
</output>
