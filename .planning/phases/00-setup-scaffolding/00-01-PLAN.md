---
phase: 00-setup-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - README.md
  - .github/workflows/ci-cd.yml
  - docs/architecture.md
  - .gitignore
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03]
must_haves:
  truths:
    - "Repository structure matches full_build_plan.md services layout"
    - "docker-compose.yml starts all services, 4 PostgreSQL instances, and RabbitMQ"
    - "README.md provides getting started instructions and architecture overview"
    - "CI/CD workflow seed created (will be completed in Phase 6)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local orchestration for all microservices and dependencies"
    - path: "README.md"
      provides: "Project overview, prerequisites, quick start, architecture diagram"
    - path: "docs/architecture.md"
      provides: "Detailed architecture explanation, service boundaries, communication patterns"
    - path: ".github/workflows/ci-cd.yml"
      provides: "Seed workflow file; Phase 6 will complete with test/build/push steps"
    - path: ".gitignore"
      provides: "Standard Python/Java exclusions and environment secrets"
  key_links:
    - from: "docker-compose.yml"
      to: "All services (user, product, order, notification)"
      via: "Service definitions reference local build paths"
    - from: "docker-compose.yml"
      to: "PostgreSQL (4 instances) and RabbitMQ"
      via: "Dependency declarations"

---

Objective
---------
Create repository structure, docker-compose orchestration, and documentation foundation. Prepare dev environment for service development.

Purpose: Enable local development and CI/CD scaffold for all phases.
Output: docker-compose.yml, README.md, architecture.md, CI/CD seed workflow.

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/initial_research.md
</execution_context>

<context>
See `full_build_plan.md` Phase 0 section for directory structure and prerequisites.
This phase is prerequisite for all other phases (they depend on docker-compose and repo layout).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure and docker-compose.yml</name>
  <files>docker-compose.yml, .gitignore</files>
  <action>
    - Create directory structure matching full_build_plan.md:
      * services/{user-service, product-service, order-service, notification-service}
      * gateway/
      * .github/workflows/
      * docs/
    - Create `docker-compose.yml` with services:
      * user-service (port 8001, PostgreSQL at localhost:5432)
      * product-service (port 8002, PostgreSQL at localhost:5433)
      * order-service (port 8003, PostgreSQL at localhost:5434)
      * notification-service (port 8004, no database)
      * api-gateway (port 80, depends on all services)
      * rabbitmq (port 5672)
      * 4 PostgreSQL instances on different ports (5432-5435)
    - Each service configured with:
      * build: context/Dockerfile for local development
      * environment vars (DATABASE_URL, JWT_SOCIRET from .env or defaults)
      * volumes for code mounting
      * depends_on: [database, rabbitmq]
    - Add `.gitignore` with:
      * __pycache__, *.pyc, venv/, node_modules/
      * target/, .class (Java)
      * .env, .env.local
      * .DS_Store
  </action>
  <verify>
    <automated>docker-compose config -q 2>&1 | head -5</automated>
  </verify>
  <done>docker-compose.yml is valid syntax; directory structure created; .gitignore present.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md and architecture.md documentation</name>
  <files>README.md, docs/architecture.md</files>
  <action>
    - Create `README.md` with sections:
      * Project overview (one paragraph)
      * Prerequisites (Docker, Python 3.11, Java 17, Git)
      * Quick start (clone, docker-compose up, curl examples)
      * Architecture diagram (ASCII or link to docs/architecture.md)
      * Tech stack table (from full_build_plan.md)
      * Design patterns and SOLID principles used
      * Phase breakdown with links
    - Create `docs/architecture.md` with:
      * Detailed architecture diagram (service boxes, arrows, databases)
      * Service responsibilities and boundaries
      * Communication patterns (HTTP REST, RabbitMQ)
      * Database-per-service pattern explanation
      * Deployment strategy (Oracle Cloud Oracle Cloud Always Free ARM A1 Compute)
      * Security considerations (JWT, secrets management)
  </action>
  <verify>
    <automated>test -f README.md && test -f docs/architecture.md && echo "Documentation files present"</automated>
  </verify>
  <done>README.md and docs/architecture.md created with sufficient detail for onboarding.</done>
</task>

<task type="auto">
  <name>Task 3: Create CI/CD workflow seed file</name>
  <files>.github/workflows/ci-cd.yml</files>
  <action>
    - Create `.github/workflows/ci-cd.yml` with:
      * Trigger on push to main and pull_request
      * Jobs outline for Python services (lint, test), Java service (build, test), Docker build (placeholder)
      * Comments indicating Phase 6 will complete with test/build/push steps
      * Structure preserved for Phase 6 to fill in actual commands
  </action>
  <verify>
    <automated>test -f .github/workflows/ci-cd.yml && echo "CI/CD workflow seed present"</automated>
  </verify>
  <done>CI/CD seed workflow created with job structure; Phase 6 will implement details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Local development machine (trusted)
git repository (trusted)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | S | .env secrets in repo | mitigate | Add .env to .gitignore; document .env.example with defaults; never commit real secrets. |
| T-02 | T | Docker network | accept | Local dev only; network isolation via compose. |
</threat_model>

<verification>
- Run `docker-compose config -q` — validates YAML syntax.
- Verify directory structure: `ls -R services/` shows all 4 service folders.
- Check .gitignore excludes .env, __pycache__, target/.
</verification>

<success_criteria>
1. docker-compose.yml is syntactically valid and starts all services (may fail on missing Dockerfiles initially, but compose orchestration works).
2. README.md provides clear quick-start instructions.
3. docs/architecture.md explains service topology and communication.
4. CI/CD workflow seed file exists with job structure.
</success_criteria>

<output>
After completion, create `.planning/phases/00-setup-scaffolding/00-01-SUMMARY.md` with directory structure and docker-compose validation.
</output>

